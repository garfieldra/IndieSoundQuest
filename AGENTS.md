# IndieSoundQuest 协作约定

## 主动推进

- 每完成一个可验收的功能、联调、排障或文档阶段，Agent 必须在交付结论中主动给出下一步建议，无需等待用户追问。
- 无论任务成功、部分完成、失败或仍在处理，面向用户的每次最终回复都必须包含当前最优先的下一步；若尚未完成，明确说明将继续执行的下一项检查或修复。
- 建议按优先级列出 1–3 项，并说明首选项为何是当前最有价值的推进方向。
- 若下一步涉及产品取舍、架构选择、外部服务成本、数据授权或不可逆操作，先明确列出待用户确认的问题；其余常规开发、测试和文档维护可直接继续。
- 建议必须基于当前项目状态、既有 Spec 和已验证结果，不重复已完成内容，也不将 Milvus 等增强能力误报为主链路。
- 对已获授权的验证、验收与缺陷修复任务，Agent 应自行完成“修改 → 重建 → 真实/自动化验证 → 必要修复 → 复验”的完整闭环；除非需要新的外部授权或产品取舍，不得把中间操作逐步抛给用户确认。
- 验收发现缺陷后，默认继续定位、修复、重建与重验收，直至终态；不得在普通技术节点等待用户逐步授权。

## 项目边界

- 仅保留两个业务 Agent：候选歌曲池生成 Agent 与赛后报告 Agent。
- 两个业务 Agent 均采用 ReAct 式自主决策；运行时护栏只用于安全、预算、契约校验与无收益循环阻断，不得退化为固定工具顺序工作流。
- **候选发现策略同样必须由 ReAct Supervisor 自主决定。** 尤其在用户未指定艺人时，Agent 应从偏好中提取可检验信号，自主选择以场景、情绪、流派、年代、语言、文化语境或相邻艺人作为网络发现切面；不得用“先查 A、再查 B”的固定搜索流水线替代决策。
- 候选歌曲池默认以在线发现为主链路：网络搜索负责发现，MusicBrainz 负责身份核验，Java 目录负责规范化落库、去重和后续赛事引用。本地目录只是可复用缓存与补充来源，不能假定它覆盖用户兴趣，也不能因为它无关而限制候选生成。
- 用户文本中的具名艺人、乐队或别名如存在多义、无法可靠解析或与多个 MusicBrainz 实体相符，必须先进入意图澄清并由用户确认；不得静默猜测后开始生成候选池。
- 网络搜索与 MusicBrainz 是歌曲发现/核验的主链路；Milvus 主题卡仅用于解释与推荐理由增强，不能限制网络发现或成为外部推荐实体来源。
