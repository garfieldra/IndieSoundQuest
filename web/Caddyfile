{$SITE_ADDRESS} {
    encode zstd gzip

    handle /api/* {
        reverse_proxy java-service:8080
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
